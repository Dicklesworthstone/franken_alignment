# Recover the topology observer without restoring old topology trust

`FileOversight::create_mediated_guarded` composes the original authority-graph gate with all existing guarded bootstraps and optional prediction/evaluation. The original credential resolver, numerical configuration, predictor and evaluator still validate their inputs. The first canonical replacement contains the entire configuration; no intermediate owner or cut certificate is returned. A graph starts uncertified. This supplies the missing topology-observer custody in the practical durable publication path, not another graph checker, evaluator or rights ledger.

`open_mediated_guarded` checks an independently retained `FileMediatedRequirements`. It pins the original graph and last registered graph separately, including every edge, provenance, perimeter declaration, generation and inventory identity. It also pins availability in the selected image, exact optional predictor/evaluator presence, every existing guard, current policy, credential state and external history floors. Graph and numerical configuration preflight precedes historical execution; full semantic replay and remaining checks precede cleanup and the single original recovery fence. Unknown or withdrawn coverage never means an empty complete topology.

Only acknowledged recovery returns `FileMediatedRoles`: the new topology observer alongside the original human, identity-observer, policy-governor and optional prediction/evaluation roles, for separate custody. The native recovery fence withdraws topology coverage and all old effect approvals. Old observer handles do not bind to the reopened owner. Recovery does not re-certify the old graph. New work requires an explicitly registered newer graph AND inventory, the original cut verification and all ordinary congress/human/publication requirements. Even a newly verified cut cannot undo a terminal stop.

Original guarded, evaluated-guarded and predictive-guarded entry points now require mediation to be absent rather than accepting unpinned topology. Their public structure layouts remain unchanged. Legacy generic recovery remains its separate profile; it still returns no topology observer. Labels, historical dispatch cuts, already published effects and unresolved charges survive according to their original ledgers. A current topology certificate is not an endpoint nonexecution receipt.

Five public regressions cover actual two-key publication before and after a single recovery, exact initial/current/availability/floor refusals without staging cleanup, new evaluator custody with retained pending labels and unresolved dispatch accounting, invalid native bootstrap paired with permitted admission, and fresh topology certification that cannot undo a terminal stop. One compile-fail doctest disallows cloning the role bundle. Existing tests and assertions are unchanged.

These tests are added sources, not passing execution evidence. RCH verification was attempted but exited 127 because `rch` is unavailable, before compilation. Rust compilation, rustfmt, Clippy and tests/doctests remain unexecuted. Source hashes and whitespace checks do not establish Rust correctness. The graph is still operator-declared evidence, not operating-system containment or authenticated topology. External floors reject old counters, not equal-counter forks. No dependency, journal encoding, Beads state or production qualification gate changes.
