# Evaluated guarded startup, recovery and canonical inspection

`FileOversight::create_evaluated_guarded` installs the original independent evaluation protocol together with every requested existing guard in the first canonical image. It reuses `PreparedGuardedBootstrap`, the original perimeter resolver, native enable transitions and sole Store replacement. A malformed protocol refuses before storage creation. Only acknowledged publication returns `FileEvaluatedOversightRoles`: the existing separately held human, identity-observer and policy-governor roles alongside the independent evaluator. No current evidence, clock, approval, provider secret or measured model identity is invented by bootstrap.

`open_evaluated_guarded` requires the independently retained `EvaluationProtocol` and the complete original `FileRecoveryRequirements`. It compares exact protocol presence and every field, as well as model/monitor/sampler and numerical-stop configuration, before numerical replay. Full original-machine replay validates all subsequent labels, promotions, controls and suffixes. All remaining guard, effective-policy, credential and external-floor checks precede staging cleanup or a recovery write. A single original recovery fence then returns all roles for separate custody. Labels, censored-label history, unfinished-round denominators, false-stop history and promoted weights remain; old file evaluator roles, tickets and effect approvals do not become usable on the reopened owner.

The original guarded entry points require evaluation to be absent rather than accepting an unchecked new protocol. Their public guard/role struct layouts are unchanged. Evaluated startup/recovery are explicit alternatives, not weaker fallbacks. Generic historical inspection and legacy recovery remain their separately documented profiles. A fresh evaluator can label retained applied reviews without fresh time, even when the numerical owner remains paused or stopped; this cannot resume it, clear a source interruption, manufacture identity evidence or grant effect authority. New weight promotion still requires current time and the original native qualifications and control preconditions. An exact retry of an already committed promotion returns its historical receipt without repeating its authority transition.

`read_credibility` validates one entire canonical image using those same exact requirements and returns the native report, native promotion history and enclosing publication snapshot. It can operate beside a locked or faulted owner without a writer lock, cleanup, fence, clock refresh or role issuance. This distinguishes canonical visibility from an acknowledgment lost during storage. Missing labels and unfinished reviews remain explicit; neither an old acknowledgment nor a convenient subset of cases substitutes for the selected canonical history. The file remains operator-controlled evidence, not a cryptographically authenticated head or protection against equal-counter forks. Native historical numerical reconstruction is additional work, not a claimed bounded-latency inspection.

Five public integration regressions cover new evaluator custody after one recovery, resolving retained censored labels before fresh-clock promotion and new two-key publication, all protocol fields and history/suffix checks beside faulted storage, stopped numerical owners, lifetime false-stop accounting across policy generations, and the complete source/identity/campaign/credential/decoder profile. The complete-profile case uses actual evidence files, original congresses, independently submitted labels, a real weight change, revocation of pending policy approval and identity eligibility, fresh identity and human review, credentialed message publication and reopening with all roles.

Four unit regressions use the original Store fault seam. Twenty replacement scenarios cover evaluated first-image publication, recovery fencing, independent label submission and qualified weight promotion at Stage, Write, FileSync, Rename and DirectorySync. They distinguish last-acknowledged from canonical state, return no speculative roles or results, reject old evaluator roles, and verify exact committed retries do not add records or promotions. Additional cases preserve the source-interruption latch and reject duplicate protocols and syntactically valid but semantically impossible label histories. One compile-fail doctest rejects snapshot-to-permit conversion. These are added test sources, not reported passing executions or hardware power-cut evidence.

The required RCH verification invocation exited 127 because `rch` is unavailable, before compilation. Rust compilation, rustfmt, Clippy and all unit/integration/doctest execution remain unverified. No native evaluator arithmetic, existing assertion, dependency, additional event encoding, Beads status or production qualification gate was changed by this integration. It advances the durable credibility and anti-compliant-helper-selection connection for plan section 9.9, not population calibration, institutional independence, authenticated labels, production runtime admission or general safety.
