# Durable prediction from the original owned decoder

`FileConsistencyConfig::with_hosted_residual` fixes the source to one actual residual tap before work. The existing predictive guarded bootstrap binds its profile, dimensions and stream against the owned decoder, in the first canonical image. Missing decoders, mismatched streams and invalid layers refuse before storage creation. The exact configuration uses version two, including the selected layer; version-one supplied-frame configurations and bytes remain unchanged. Existing predictive recovery compares the whole configuration before numerical replay, so it cannot silently switch between these source modes or select another layer.

`FileConsistencyObserver::forecast_hosted_request` binds the original request allocator's next attempt to an external actor key. `forecast_hosted_action` supports the existing explicit attempt path. Both are separately held observer operations, not actor-port methods. Their journal commands contain only the attempt/request and expected actor revision, never activation values, a claimed probability, a sequence override or a replacement model. Replay reconstructs the current selected residual from the original numerical history and runs the original predictor. Input-family 28 subtags four and five are new; all earlier tags retain their encoding.

The selected residual is the actual completely reviewed capture retained by the numerical owner, not another computation on a caller-selected model. Both raw-frame forecast interfaces refuse while the owned mode is active, even for matching metadata or an exact copied frame. Whole-history reconstruction by the durable transaction remains extra numerical work; this change adds no second forward pass merely to obtain a current residual. The original pre-computation unavailable latch covers these commands, and only acknowledged replacement exposes their result and keyed binding. Classification, downstream refusals, exact retries and effect-accounting semantics remain those of the original request book and likelihood process.

At a clean recovery boundary, the original source mode, likelihood product, consumed prediction allowance and source-sequence floor survive. New work requires fresh time, explicit numerical resume and a strictly newer actual token before the next forecast. An unanswered forecast instead remains recorded and becomes lost coverage. Reopening never turns it into an omitted trial. Numerical checkpoint reset still changes the decoder stream; the fixed-stream predictor has no migration/rearming protocol, so it cannot silently start forecasting from that reset stream or reopen the raw-frame path.

Six new public integration regressions cover the actual actor gateway through owned forecasting, full-input congress, separate human approval, publication and reconciliation; same-metadata frame substitution and pinned source-mode/layer recovery; a second sampled numerical continuation and publication after clean recovery; threshold crossing with a retained refused request and endpoint sealing before refund; held/stopped owners and real staged-file storage refusal; and invalid atomic bootstrap with explicit legacy controls. Two unit regressions independently assemble the version-two extension and the frame-free command vectors, check every truncation, and reject illegal suffixes. These tests are added sources, not passing execution evidence. Existing tests and assertions are unchanged.

The RCH command for `file_owned_forecasts` and `hosted_action_forecasts` exited 127 before compilation because `rch` is unavailable. Rust compilation, rustfmt, Clippy, unit/integration tests and doctests remain unexecuted. Source-identity and Git whitespace checks are not Rust qualification. This connects the CPU reference decoder to the persistent pre-action consistency path for plan section 12.7 / FA-111. It establishes neither trained-model calibration, real deployment isolation, cryptographic source/parameter authentication, wall-clock bounds nor absence of future information outside these interfaces. Independently known capture gaps that never committed still require operator handling; canonical recovery cannot prove them absent. No dependencies, existing assertions, Beads statuses or production gates change.
