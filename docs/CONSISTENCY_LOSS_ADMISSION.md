# Capture-loss admission before journal encoding

A separately held `FileConsistencyObserver` reporting `unavailable` closes the acknowledged live owner before journal-capacity checks, encoding, semantic replay, or persistence. A failure to record an already known capture gap must not leave the older quiet likelihood history usable. Only acknowledgment of the original transaction clears the unavailable latch; the native coverage-loss state then blocks effects. Foreign roles, stale revisions, and an unconfigured consistency lane refuse before this boundary.

The last acknowledged journal remains historical after failure. Neither a disabled owner nor a staged file proves that a loss record reached disk, that an effect was stopped, or that a dispatched effect did not execute. Reopening cannot reconstruct a gap absent from the canonical image; an independently known uncommitted gap must still be supplied by the operator. Recovery preserves the original cancellation, fencing and unresolved-liability rules.

Three regression functions cover exact event exhaustion versus the one-event-larger acknowledgment control, foreign/stale observations followed by actual two-key publication, and staged-write failure retaining an unresolved dispatch through native recovery and reconciliation. They are source additions, not reported passing executions. RCH was attempted but is unavailable (exit 127 before compilation). Compilation, rustfmt, Clippy and Rust test execution remain unverified. No dependency, journal encoding, existing assertion, Beads status or qualification gate changes.
