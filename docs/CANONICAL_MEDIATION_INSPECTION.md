# Canonical mediation evidence beside a faulted owner

`FileOversight::read_mediation` inspects a complete canonical image using the exact composed mediation recovery requirements. It returns the native topology availability, verified cut and latest check result together with the original publication snapshot and optional prediction/credibility reports. It never obtains a writer lock, removes staging files, appends a fence, refreshes a clock, invokes an observer, recertifies a graph or returns a role. Its consumer is an operator resolving an ambiguous topology-change or cut-check acknowledgment before deciding how to recover.

Every retained topology update includes its exact request, original journal revision and native `Option<TopologyChange>`. A recorded None is the original no-op withdrawal of already unavailable coverage; it is not a missing update or an endpoint nonexecution claim. An update absent from the selected image is absent from that history, not proof that no external capture gap occurred. Operators explicitly pin the known old or intended new graph when inspecting alternative outcomes of an ambiguous write; the reader does not bless arbitrary bytes as a new configuration. Historical dispatch cuts and executed/unknown liabilities remain in their original ledgers.

The reader reuses the same preflight and full replay as mediated recovery, without its writes. Exact original/current graphs, numerical and predictor configuration, optional evaluator, all existing guards, policy/credential state and external counter floors are checked. A syntactically valid but impossible suffix invalidates the whole result. Numerical replay is additional computation, not a bounded-latency or measured-overhead promise. Files remain operator-controlled evidence; exact data comparison is neither source authentication nor detection of every equal-counter fork.

Two public integration regressions cover canonical inspection beside a faulted owner, distinct recorded withdrawal/no-op/missing updates, preserved executed charges, fresh topology and two-key publication after recovery, and the combined owned-decoder/prediction/evaluation/mediation path. The latter uses the original numerical decoder and registered predictor, actual congress/human publication, retained independent labels and lifetime evidence, a single recovery, a newer graph/cut, explicit numerical resume and a newly sampled token before another forecast and effect. Quiet predictions and graph cuts never substitute for congress or human approval.

Four unit regressions add twenty original Store replacement-barrier scenarios across first-image publication, recovery fencing, cut certification and topology replacement. They distinguish canonical visibility from last acknowledgment, reject old roles, preserve native exact retries without another record or restored coverage, and reject duplicate bootstraps and impossible suffixes without cleanup. One compile-fail doctest forbids report-to-permit conversion. Positive recovery and certification controls accompany storage refusals.

RCH verification was attempted but exited 127 because rch is unavailable, before compilation. All added unit, integration and doctest sources remain unexecuted, and compilation, rustfmt and Clippy remain unverified. No existing assertions or native checker/evaluator/authority algorithms are changed. No dependency, journal encoding, Beads closure, OS-isolation, calibrated-model or production qualification claim is introduced.
